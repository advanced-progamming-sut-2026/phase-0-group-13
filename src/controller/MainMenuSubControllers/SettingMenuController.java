package controller.MainMenuSubControllers;

import controller.BaseController;

public class SettingMenuController implements BaseController {
    @Override
    public void initController() {
        // phase one: initialize settings menu controller
    }

    public void handleinput(String command) {
        // phase one: parse and route settings commands
    }

    @Override
    public void exit() {
        // phase one: close settings controller
    }
}
